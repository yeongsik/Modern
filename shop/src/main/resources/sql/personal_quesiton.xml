<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PersonalQuestion">
	
	<!-- 게시물 수 -->
    <select id="question_list_count" resultType="int">
    	select count(question_id) from personal_question
    </select>
    
    <!-- 게시판 목록 -->
    <select id="question_list" parameterType="int" resultType="personal_question">
   	 	<![CDATA[
		select * from 
		 (select rownum rnum, question_id, question_content, question_subject, question_date, question_state, question_type
		 from (select * from personal_question order by question_id desc)) 
	   		 where rnum >= 1
		]]>
    </select>

</mapper>
